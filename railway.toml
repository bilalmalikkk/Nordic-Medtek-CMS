[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration for persistent database storage and uploads
[[deploy.volumes]]
name = "cms-storage"
mountPath = "/data"

